# 小米摄像头录像自动合并工具 v3.0 · 部署与操作文档

> 部署目标：群晖 NAS（DSM 7.x，x86/amd64 架构；示例中的 NAS 地址与设备序列号均已脱敏，部署时请替换为你实际的 NAS 地址与设备标识）
> 文档版本：v3.0　|　更新日期：2026-09-22

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构与工作原理](#2-系统架构与工作原理)
3. [目录结构与文件说明](#3-目录结构与文件说明)
4. [摄像头存储结构说明](#4-摄像头存储结构说明)
5. [环境准备（前置条件）](#5-环境准备前置条件)
6. [本地开发与测试（可选）](#6-本地开发与测试可选)
7. [NAS 部署步骤](#7-nas-部署步骤)
8. [生产配置说明（环境变量表）](#8-生产配置说明环境变量表)
9. [定时任务配置（每天 00:00 / 12:00 / 20:00 自动化）](#9-定时任务配置每天-0000--1200--2000-自动化)
10. [运行验证与日志解读](#10-运行验证与日志解读)
11. [运维手册](#11-运维手册)
12. [常见问题排查](#12-常见问题排查)
13. [安全与注意事项](#13-安全与注意事项)
14. [附录：本次部署记录](#14-附录本次部署记录)

---

## 1. 项目概述

### 1.1 背景

小米摄像头会把录像以 **约 19 分钟一段** 的 `.mp4` 小分段保存到群晖 NAS 的 CCTV 共享文件夹中。原始录像存在以下问题：

- **文件数量庞大**：两个点位每天产生约 150+ 个分段文件，几天就堆积上千个；
- **不易回放**：每个文件只有 19 分钟，播放历史录像需要逐个点开；
- **管理困难**：文件平铺在摄像头目录下，无法按时间线检索。

### 1.2 目标（v3.0）

在 NAS 上部署一个自动合并工具，实现：

1. **约 10 个一段合并**：把同一摄像头（或同一通道）按时间顺序每 10 个分段合并成一个视频（`GROUP_SIZE=10`）；不足 `MIN_GROUP_SIZE=8` 个时等待下次运行积攒，避免产生零碎小文件；
2. **自动归档**：按 `merged/<年>/<月>/<日>/<组内首段开始时间14位>_<通道短名>.mp4` 组织，按年月日浏览，文件名即时间顺序；
3. **双通道分别合并**：`XiaomiCamera_01_xxx` 下的 `0`（主码流）与 `1`（副码流）是两个独立通道，分别合并、分别命名（短名 `01_0` / `01_1`），绝不混流；
4. **自动删除源文件**：合并成功后删除已合并的分段文件，释放磁盘空间；
5. **每天固定时间自动运行**：每天 **00:00、12:00、20:00** 各自动合并一次，**不重复合并、不重复备份**；
6. **保留手动合并按钮**：可随时在「任务计划」中点「运行」手动触发一次。

### 1.3 最终成果（已上线运行）

| 项目 | 状态 |
|---|---|
| NAS 项目目录 | `/volume1/docker/xiaomi-camera-merge`（源码 + compose） |
| Docker 镜像 | `xiaomi-camera-merge:local`（v3.0，2026-09-22 15:13 构建成功） |
| v3.0 首次运行 | **处理 3 组：成功 3 组，失败 0 组**（输出 `merged/2026/09/…`，删源正常） |
| 定时任务 | `xiaomi-camera-merge-task`（每天 00:00）+ `xiaomi-camera-merge-task-1200`（每天 12:00）+ `xiaomi-camera-merge-task-2000`（每天 20:00），均 root 运行 |
| 合并输出 | `/volume1/CCTV/merged/<年>/<月>/<日>/<时间戳>_<短名>.mp4` |
| 运行日志 | `/volume1/CCTV/xiaomi-video-merge-log/` |

---

## 2. 系统架构与工作原理

```
小米摄像头（两个点位）
   ├── 点位1：XiaomiCamera_00_XXXXXXXXXXXX   （单码流，文件平铺 → 短名 00）
   └── 点位2：XiaomiCamera_01_XXXXXXXXXXXX   （双通道：0=主码流、1=副码流 → 短名 01_0 / 01_1）
                 ↓ 通过小米/群晖联动持续写入
群晖 NAS · CCTV 共享文件夹（/volume1/CCTV）
   └── 每约 19 分钟产生一个 .mp4 分段（00_20260922091818_20260922093706.mp4）
                 ↓ 每天 00:00 / 12:00 / 20:00 被「任务计划」拉起
Docker 容器 xiaomi-camera-merge（一次运行后自动退出）
   └── 遍历 → 按【拍摄单元(摄像头/通道)】分组
        → 单元内按开始时间排序 → 每 10 个一段合并（不足 8 个等待）
        → ffmpeg concat 流复制合并
                 ↓
merged/<年>/<月>/<日>/<组内首段开始时间14位>_<短名>.mp4   （归档）
   + 删除已合并的源文件（DELETE_SUCCESS=true）
   + 运行日志写入 xiaomi-video-merge-log/<时间戳>.log
```

### v3.0 分组规则（相对旧版的核心变化）

| 维度 | v2.0（旧） | v3.0（当前） |
|---|---|---|
| 分组依据 | 摄像头 + 开始小时 | 拍摄单元（摄像头/通道）+ 时间顺序，每 10 个一组 |
| 组大小 | 1 小时内的所有文件 | `GROUP_SIZE=10`（默认 10 个一组） |
| 最后一组下限 | —（不足一组则跳过） | `MIN_GROUP_SIZE=8`（≥8 个就合成一组，<8 个等待下次） |
| 跨小时 | 不跨小时 | 允许跨小时（按时间顺序连续取 10 个） |
| 输出路径 | `merged/<摄像头>/<年>/<月>/<日>/<时>.mp4` | `merged/<年>/<月>/<日>/<组首时间戳14位>_<短名>.mp4` |
| 短名 | 完整摄像头目录名 | 平铺→`00`；双通道→`01_0`/`01_1` |

### 合并原理

- 使用 `ffmpeg -f concat -safe 0 -i files.txt -c copy` 进行**流复制（不重新编码）**合并；
- 优点：速度快（实测约 1000 倍速，一个 55 分钟视频约 8 秒完成）、不损失画质、不占额外 CPU/内存；
- 先输出到临时文件再重命名归档，合并失败不会留下残缺文件。

---

## 3. 目录结构与文件说明

### 3.1 NAS 端项目目录 `/volume1/docker/xiaomi-camera-merge/`

| 文件 | 大小 | 说明 |
|---|---|---|
| `main.go` | 约 23 KB | 合并工具主程序（v3.0，已含详细中文注释） |
| `lib/log/log.go` | 约 1.4 KB | 简易日志库（写运行日志） |
| `Dockerfile` | 约 2.2 KB | 多架构镜像构建文件（amd64/arm64） |
| `go.mod` | 512 B | Go 模块定义（无第三方依赖） |
| `.dockerignore` | 约 955 B | 构建上下文排除清单 |
| `docker-compose.yml` | 约 2.1 KB | 生产部署编排（环境变量/挂载） |
| `synology-task-scheduler.sh` | 约 2 KB | 定时任务脚本（已部署为三个任务计划的内容） |

> ⚠️ 注意：`lib/` 目录下只允许存在 `log/log.go` 一个 Go 文件。不要把 `main.go`、`docker-compose.yml`、`synology-task-scheduler.sh` 误传到 `lib/log/` 下，否则 Go 编译会报 “found packages log and main” 包冲突错误。

### 3.2 运行期目录（在 CCTV 共享文件夹内）

| 路径 | 说明 |
|---|---|
| `CCTV/XiaomiCamera_00_xxx/` | 摄像头 00 原始分段（处理后源文件会被删除） |
| `CCTV/XiaomiCamera_01_xxx/0`、`/1` | 摄像头 01 的两个通道原始分段 |
| `CCTV/merged/<年>/<月>/<日>/<时间戳>_<短名>.mp4` | **v3.0 合并归档输出** |
| `CCTV/merged/XiaomiCamera_00_xxx/…`、`XiaomiCamera_01_xxx/…` | v2.0 旧归档（历史产物，不再写入） |
| `CCTV/xiaomi-video-merge-log/<时间戳>.log` | 每次运行的日志 |

---

## 4. 摄像头存储结构说明

实测 CCTV 共享文件夹的真实结构：

```
/volume1/CCTV/
├── XiaomiCamera_00_XXXXXXXXXXXX/     ← 点位1：平铺结构（约 1143 个分段）
│     00_20260922091818_20260922093706.mp4
│     ...
├── XiaomiCamera_01_XXXXXXXXXXXX/     ← 点位2：双通道结构
│   ├── 0/                            ← 主码流（短名 01_0）
│   │     01_20260917065032_20260917070958.mp4
│   └── 1/                            ← 副码流（短名 01_1）
├── merged/                           ← 合并输出（工具自动创建）
├── xiaomi-video-merge-log/           ← 运行日志
└── #recycle/                         ← 回收站（自动跳过，不处理）
```

- **文件命名**：`<前缀>_<开始时间YYYYMMDDHHMMSS>_<结束时间YYYYMMDDHHMMSS>.mp4`；
- **工具自适应**：摄像头目录下直接有 mp4 → 整个目录为一个合并单元（短名取 `00`）；否则按通道子目录（0/1）分别作为合并单元（短名 `01_0` / `01_1`）；
- **两通道分别合并**：主码流与副码流分辨率/码率不同，绝不能混流，因此通道 0、通道 1 各自独立合并、独立命名；
- **回收站与隐藏目录**：`#`、`.` 开头的目录一律跳过；
- **输出目录/日志目录**：`merged`、`xiaomi-video-merge-log` 自动跳过，不参与合并。

---

## 5. 环境准备（前置条件）

| 前置条件 | 说明 |
|---|---|
| 群晖 NAS | x86/amd64 架构机型，DSM 7.x 以上 |
| Container Manager 套件 | 套件中心安装（本机 2026-09-18 已装） |
| CCTV 共享文件夹 | 小米摄像头 NAS 存储根目录（/volume1/CCTV） |
| 摄像头接入 | 小米摄像头已通过联动把录像写入 CCTV（目录结构见第 4 节） |
| 管理员账号 | 需要能创建任务计划、运行 Docker 的账号（本方案用 root） |

> 无需在 NAS 上安装 Go 或 ffmpeg —— 全部打包在 Docker 镜像内。

---

## 6. 本地开发与测试（可选）

仅在需要修改工具逻辑时使用。

```powershell
# 编译（Windows，需 Go 1.19+）
go build -o xiaomi_camera_merge.exe .

# 本地测试（需安装 ffmpeg 并加入 PATH）
xiaomi_camera_merge.exe -path <本地测试目录> -delete -max_merge 2 -group_size 10 -min_group_size 8
```

本地测试目录模拟结构：

```
test_root/
├── XiaomiCamera_00_XXXXXXXXXXXX/00_20260901000100_...mp4   （平铺 → 短名 00）
└── XiaomiCamera_01_XXXXXXXXXXXX/0/…、/1/…                  （双通道 → 短名 01_0 / 01_1）
```

---

## 7. NAS 部署步骤

### 7.1 创建项目目录并上传源码

1. 打开 **File Station**；
2. 进入 `/docker`，新建文件夹 `xiaomi-camera-merge`；
3. 依次上传：`main.go`、`go.mod`、`Dockerfile`、`.dockerignore`、`lib/`（内含 `log/log.go`）、`docker-compose.yml`；
4. ⚠️ 上传后核对：`lib/log/` 下**只能有** `log.go`，不能有多余文件（见第 3.1 节注意）。

### 7.2 在 Container Manager 创建项目

1. 打开 **套件中心 → Container Manager**；
2. 进入 **项目** → 点击 **新增**；
3. 项目名称填 `xiaomi-camera-merge`；
4. 设置路径时浏览到 `/docker/xiaomi-camera-merge`（DSM 检测到已含 `docker-compose.yml`，会自动使用它创建项目）；
5. 确认预览显示的 compose 内容正确；
6. 勾选「创建项目后立即启动」，完成创建。

### 7.3 构建镜像并首次运行

创建项目后 Container Manager 会自动执行构建（或手动：项目详情 → 操作 → 构建）：

```
Step 1/14 : FROM golang:1.22-alpine AS builder
...
Successfully built 7f3786dd23e9
Successfully tagged xiaomi-camera-merge:local
Container xiaomi-camera-merge Started
Exit Code: 0
```

- 构建过程：Go 编译 → Alpine 基础镜像 + ffmpeg（安装约 111 个依赖包，耗时约 40 分钟，仅首次）；
- 构建成功后容器自动运行一次合并任务；因为是**一次性任务**，跑完自动停止（`restart: "no"`）。

### 7.4 验证输出

用 File Station 浏览（v3.0 输出结构）：

```
/volume1/CCTV/merged/2026/09/01/
  20260901191847_00.mp4   （1.2 GB，平铺摄像头 00 的合并视频）
  20260901203034_00.mp4   （987 MB）
/volume1/CCTV/merged/2026/09/22/
  20260922095750_01_1.mp4 （1.23 GB，双通道摄像头 01 的通道 1）
```

---

## 8. 生产配置说明（环境变量表）

当前 `docker-compose.yml`（生产配置）：

```yaml
environment:
  - TZ=Asia/Shanghai        # 时区（影响“当前小时”判断与归档路径）
  - DELETE_SUCCESS=true     # 合并成功后删除源文件
  - MAX_MERGE=0             # 0 = 不限制单次合并组数
  - VIDEO_EXT=mp4           # 待合并扩展名
  - MIN_AGE_MIN=30          # 修改时间晚于 30 分钟前的文件不处理
  - SKIP_CURRENT=true       # 跳过开始时间在当前小时的文件
  - GROUP_SIZE=10           # 每组文件数（约 10 个一段）
  - MIN_GROUP_SIZE=8        # 最后一组下限文件数（不足 8 个等待下次）
volumes:
  - /volume1/CCTV:/app/video   # CCTV 共享文件夹挂载
```

| 变量 | 默认 | 生产值 | 作用 |
|---|---|---|---|
| `DELETE_SUCCESS` | false | **true** | 合并成功后删除已合并源文件（释放空间） |
| `MAX_MERGE` | 0 | **0** | 单次最多合并组数；0=不限制；积压极大时可设 30~50 分批 |
| `VIDEO_EXT` | mp4 | mp4 | 待合并文件扩展名 |
| `OUTPUT_DIR` | merged | merged | 输出目录名（位于视频根目录下） |
| `MIN_AGE_MIN` | 30 | **30** | 只处理修改时间早于该分钟数的文件（防合并到写入中的文件） |
| `SKIP_CURRENT` | true | **true** | 跳过开始时间在当前小时的文件（该小时可能仍在写入） |
| `GROUP_SIZE` | 10 | **10** | 每组文件数（用户要求“10 个左右一合并”） |
| `MIN_GROUP_SIZE` | 8 | **8** | 最后一组下限：≥8 合成一组，<8 等待积攒 |

> 修改配置：本地改 `docker-compose.yml` → File Station 覆盖上传 → Container Manager 项目详情 → 操作 → 构建（重新创建容器生效）。

---

## 9. 定时任务配置（每天 00:00 / 12:00 / 20:00 自动化）

容器是一次性任务，需用群晖「任务计划」定时拉起。**本方案已在 NAS 完成配置**（3 个任务），步骤记录如下（供重建/迁移参考）。

### 9.1 创建任务（每个时间点一个任务）

1. **控制面板 → 任务计划 → 新增 → 计划的任务 → 用户定义的脚本**；
2. **常规**页：任务名称填英文名（如 `xiaomi-camera-merge-task-1200`），用户账号选 `root`（运行 Docker 需要），勾选「已启动」；
   - ⚠️ 实测 DSM 对部分中文任务名会校验失败（提示“部份设置错误”），一律使用英文名；
   - 以 root 创建/保存会弹出密码验证，输入 DSM 账户密码确认；
3. **计划**页：日期选「在以下天中运行 / 每天」；开始时间填对应时刻（00:00 / 12:00 / 20:00）；**不要勾选**「在同一天内继续运行」（否则会变成周期性重复）；
4. **任务设置**页：「用户定义的脚本」填入 9.2 的脚本；
5. 确定，输入密码完成创建。

### 9.2 脚本内容（与部署一致）

```sh
#!/bin/sh
# 小米摄像头录像合并 - 群晖任务计划脚本（定时自动运行）
# 每天 00:00 / 12:00 / 20:00 各执行一次；防重叠、防重复合并
# ====================================================================
# 防重叠：若已有合并容器正在运行，本次直接跳过（避免同时合并冲突）
if docker ps --format '{{.Names}}' | grep -qx 'xiaomi-camera-merge'; then
  exit 0
fi
# 清理上一次运行退出后的残留容器（--rm 正常会自动清理，这里兜底）
docker rm -f xiaomi-camera-merge >/dev/null 2>&1
# 启动一次性合并容器（运行完自动删除）
docker run --rm --name xiaomi-camera-merge \
  -e TZ=Asia/Shanghai \
  -e DELETE_SUCCESS=true \
  -e MAX_MERGE=0 \
  -e VIDEO_EXT=mp4 \
  -e MIN_AGE_MIN=30 \
  -e SKIP_CURRENT=true \
  -e GROUP_SIZE=10 \
  -e MIN_GROUP_SIZE=8 \
  -v /volume1/CCTV:/app/video \
  xiaomi-camera-merge:local
```

> 说明：脚本同时保存在本地工程 `synology-task-scheduler.sh`，与三个任务计划的内容一致。

### 9.3 防重复合并与防重复备份机制

| 机制 | 实现 | 说明 |
|---|---|---|
| 防重叠 | 脚本开头 `docker ps` 检查 | 若上一轮合并还没跑完，本次跳过，避免两个容器同时读写 |
| 防重复合并 | 程序内“输出已存在则跳过” | 目标文件已存在（说明该组已合并过），直接跳过不重复处理 |
| 防重复备份 | 任务固定每天 3 次 + 跳过已存在 | 已合并的组不会再次合并；源文件合并后已删除，天然不产生重复备份 |

### 9.4 任务状态核对（当前 NAS 上 4 条任务）

| 任务名称 | 应用程序 | 计划 | 下次运行时间 | 拥有者 | 状态 |
|---|---|---|---|---|---|
| xiaomi-camera-merge-task-2000 | 用户定义的脚本 | 每天 20:00 | 2026-09-22 20:00 | root | 已启动 |
| xiaomi-camera-merge-task | 用户定义的脚本 | 每天 00:00 | 2026-09-23 00:00 | root | 已启动 |
| PowerOn task 0 | 电源打开/关闭 | 每天 00:00 | 2026-09-23 00:00 | root | 已启动（原有任务，勿动） |
| xiaomi-camera-merge-task-1200 | 用户定义的脚本 | 每天 12:00 | 2026-09-23 12:00 | root | 已启动 |

### 9.5 手动合并按钮

**控制面板 → 任务计划 → 选中任一 `xiaomi-camera-merge-task*` 任务 → 点击顶部「运行」按钮**，即手动触发一次合并。

> 手动运行与定时运行共用同一脚本，同样受“防重叠 + 跳过已存在”保护，可放心使用。

---

## 10. 运行验证与日志解读

### 10.1 v3.0 首次生产运行结果（2026-09-22 15:13，构建成功后自动执行）

日志 `20260922_151303.log` 关键内容：

```
小米摄像头视频合并工具 v3.0
每组文件数（默认10个左右）： 10
最后一组下限文件数： 8
跳过：单元 XiaomiCamera_01_XXXXXXXXXXXX/0 剩余 5 个文件不足一组（下限 8），等待下次运行积攒
跳过：单元 XiaomiCamera_01_XXXXXXXXXXXX/1 剩余 2 个文件不足一组（下限 8），等待下次运行积攒
组 XiaomiCamera_00_XXXXXXXXXXXX 开始 20260901191847 合并成功： video/merged/2026/09/01/20260901191847_00.mp4
已删除 10/10 个源文件
组 XiaomiCamera_00_XXXXXXXXXXXX 开始 20260901203034 合并成功： video/merged/2026/09/01/20260901203034_00.mp4
已删除 8/8 个源文件
组 XiaomiCamera_01_XXXXXXXXXXXX/1 开始 20260922095750 合并成功： video/merged/2026/09/22/20260922095750_01_1.mp4
已删除 10/10 个源文件
本次共处理 3 组：成功 3 组，失败 0 组，跳过 0 组
```

验证要点：

- **版本**：v3.0 ✓
- **每组 10 个**：前两组 10 个/组、第三组 8 个（达到下限）✓
- **不足 8 个等待**：0 通道剩 5 个、1 通道剩 2 个，均跳过等待下次 ✓
- **双通道分别合并**：`01_0`（跳过等待）、`01_1`（合并 1 组）独立处理 ✓
- **年/月/日目录 + 时间戳_短名**：`merged/2026/09/01/20260901191847_00.mp4`、`merged/2026/09/22/20260922095750_01_1.mp4` ✓
- **删除源**：`已删除 10/10`、`8/8` ✓
- **跨小时分组**：组 `20260922095750`（01:57 开始）跨小时连续取 10 个 ✓

### 10.2 查看运行日志

- 图形界面：Container Manager → 容器 → `xiaomi-camera-merge` → 日志 标签；
- 文件日志：File Station → `/volume1/CCTV/xiaomi-video-merge-log/<时间戳>.log`；
- 每次运行一个独立日志文件，可按时间戳追溯。

### 10.3 验证合并产物

File Station 浏览 `merged/<年>/<月>/<日>/` 下的 mp4，直接播放验证时长、画面正常。

---

## 11. 运维手册

### 11.1 手动触发一次合并

方式一（推荐）：控制面板 → 任务计划 → 选中 `xiaomi-camera-merge-task` → 点击顶部「运行」。

方式二（SSH/终端，需管理员）：执行任务计划里的 `docker run` 命令即可（见 9.2）。

### 11.2 查看/清理空间

- 合并前确认：`File Station → CCTV → 摄像头目录` 中的分段文件数量在减少；
- 合并后空间自动释放（源文件删除）；输出 `merged/<年>/<月>/<日>/` 才是最终保留的视频。

### 11.3 重新合并某组（输出损坏需要重做）

删除对应的归档文件后下次运行会自动重新合并：

```
删除 merged/<年>/<月>/<日>/<时间戳>_<短名>.mp4
```

> ⚠️ 注意：若源文件已被删除（DELETE_SUCCESS=true），无法重新合并，请谨慎操作删除归档。

### 11.4 暂停自动合并

控制面板 → 任务计划 → 取消勾选对应任务「已启动」即可（可单独暂停某个时段）。

### 11.5 迁移/重建

- 源码与 compose 均在 `/volume1/docker/xiaomi-camera-merge/`，按第 7 节重建即可；
- 镜像可在 Container Manager「映像」中导出备份。

---

## 12. 常见问题排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 日志提示“未找到 ffmpeg” | 本机直接运行但未装 ffmpeg | 使用 Docker 镜像运行（镜像内已带 ffmpeg） |
| ffmpeg 输出 `Non-monotonic DTS` 警告 | 分段间时间戳轻微不连续（小米摄像头常见） | 属正常现象，`-c copy` 流复制不影响成片，可忽略 |
| “输出已存在”跳过 | 该组已合并过 | 属正常去重（防重复合并）；如需重做见 11.3 |
| “不足一组等待积攒” | 单元剩余文件 < MIN_GROUP_SIZE（8） | 属设计行为：等下次定时运行凑够 10 个左右再合并 |
| 创建任务提示“部份设置错误” | 部分中文任务名校验失败 | 改用英文/数字任务名（如 xiaomi-camera-merge-task-1200） |
| 任务运行但未合并 | 有合并容器在运行（防重叠跳过）或文件仍在写入 | 属设计行为；查看日志确认 |
| 容器状态“已停止” | 一次性任务运行完自动退出 | 正常现象，定时任务会再次拉起 |
| 空间不释放 | DELETE_SUCCESS=false | 改为 true 并重新构建项目 |
| 构建报 “found packages log and main” | `lib/log/` 下误传了 main.go 等文件 | 删除 `lib/log/` 下多余文件，仅保留 log.go，重新构建 |
| 构建报 “Top-level object must be a mapping” | docker-compose.yml 注释行缺 `#` 前缀 | 检查 compose 每行注释必须 `#` 开头 |

---

## 13. 安全与注意事项

1. **删除不可逆**：`DELETE_SUCCESS=true` 会在合并成功后删除源分段文件，务必确认合并产物可正常播放后再长期开启；
2. **不要动 `#recycle`**：回收站目录由系统管理，工具会自动跳过；
3. **不要点 Container Manager「总览 → 移除未使用映像」**：可能误删 NAS 上其他项目依赖的镜像；
4. **不要手动操作 `merged/` 之外的新目录**：工具只写 `merged/` 与 `xiaomi-video-merge-log/`；
5. **任务计划以 root 运行**：账号密码验证是 DSM 正常安全机制；
6. **合并过程中请勿通过 File Station 重命名/删除 CCTV 下的文件**，避免读写出错；
7. **双通道不混流**：`0`/`1` 通道分辨率码率不同，工具已分别合并，请勿手工把两通道视频拼接。

---

## 14. 附录：本次部署记录

| 时间 | 事件 |
|---|---|
| 2026-09-22 09:33~09:46 | 上传源码（v2.0 / Dockerfile / go.mod / lib）至 `/docker/xiaomi-camera-merge` |
| 2026-09-22 10:20~10:25 | 首次测试构建成功，MAX_MERGE=2 运行：合并 2 组成功，源文件保留 |
| 2026-09-22 10:33~11:09 | 生产配置全量合并：**513 组，成功 511、失败 0、跳过 2**，源文件删除生效 |
| 2026-09-22 ~11:30 | 创建定时任务 `xiaomi-camera-merge-task`（原每小时，后改为每天 00:00） |
| 2026-09-22 12:00 | 定时任务 12:00 首次运行，日志正常 |
| 2026-09-22 13:40~13:56 | 改造 v3.0：按拍摄单元+每 10 个一组、年/月/日目录+时间戳_短名；修复 compose 注释、清理 lib/log 误传文件 |
| 2026-09-22 14:00~15:13 | v3.0 镜像构建成功（`Successfully built 7f3786dd23e9`），容器自动运行首次合并：**3 组全部成功** |
| 2026-09-22 15:13~15:40 | 定时任务改造：新建 `xiaomi-camera-merge-task-1200`（12:00）、`xiaomi-camera-merge-task-2000`（20:00），原任务改为每天 00:00 并更新为 v3.0 脚本 |
| 2026-09-22 15:40 | 输出目录 `merged/2026/09/01`、`merged/2026/09/22` 验证通过；操作文档更新至 v3.0 |

---

*文档完。如有问题，可随时根据本文档第 11、12 节排查，或联系维护人员。*
